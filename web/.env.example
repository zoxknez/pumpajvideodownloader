### Pumpaj Web – environment template
# Kopiraj u `.env.local` ili `.env.production` i popuni vrednosti pre build/deploy-a.

# API baza (npr. http://localhost:5176 ili https://api.pumpaj.app)
NEXT_PUBLIC_API=
# Supabase projekat URL (obavezno za prijavu na web-u)
NEXT_PUBLIC_SUPABASE_URL=
# Supabase anon key
NEXT_PUBLIC_SUPABASE_ANON_KEY=
