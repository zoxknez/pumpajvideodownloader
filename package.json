{
  "name": "pumpaj-web-root",
  "private": true,
  "version": "0.2.0",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "start": "npm run start -w web",
    "lint": "npm run lint -w web --if-present",
    "test": "npm run test -w web --if-present",
    "typecheck": "npm run typecheck -w web --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "overrides": {
    "@types/node": "20.19.19"
  }
}
